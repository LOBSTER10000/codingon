// 구조분해 할당(=연산자)
// -> 구조를 분해해서 할당하겠습니다
// 1. 객체({})를 구조 분해

const { choco, vanilla, orange } = {
  choco: '초코맛 쿠키',
  vanilla: '바닐라맛 쿠키',
  orange: '오렌지맛 쿠키',
};

console.log(choco);
console.log(vanilla);
console.log(orange);

// 2. 배열([])을 구조 분해
